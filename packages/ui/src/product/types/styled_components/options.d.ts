type StyleOptionExpList = {
  $length: number;
};

type StyleOptionExpItem = {
  $totalLength: number;
};

type StyleOptionCareerDetailWrap = {
  $height: number;
};

type StyleOptionDetailInfoItem = {
  $itemIndex: number;
  $delayIndex: number;
};

type StyleOptionTooltipBox = {
  $section: "footer" | "gnbUtilBtn";
  $pos: string[];
};

type StyleOptionTooltipText = {
  $section: "footer" | "gnbUtilBtn";
};
