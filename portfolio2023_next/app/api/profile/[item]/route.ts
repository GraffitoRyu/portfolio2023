// import { redirect } from "next/navigation";

// export async function GET({ params }: { params: { item: string } }) {
//   const { item } = params;
//   redirect(`${process.env.NEXT_PUBLIC_FIREBASE_DATABASE_URL}/${item}.json`);
// }
